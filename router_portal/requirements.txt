# Core Django + DRF
Django==4.2.16
djangorestframework==3.15.2
gunicorn==23.0.0

# M-Pesa Integration
requests==2.31.0
python-decouple==3.8
django-daraja==1.3.0

# Redis + Celery (if used)
redis==5.2.1
celery==5.4.0
channels==4.2.0
channels_redis==4.2.1

# Optional Admin UI Enhancements
django-admin-interface==0.29.4
django-jazzmin==3.0.1
django-crispy-forms==2.3
django-widget-tweaks==1.5.0
django-colorfield==0.12.0

# Cross-Origin Resource Sharing (if you need API access from other domains)
django-cors-headers==4.7.0

# PostgreSQL if using Render PostgreSQL database
psycopg2-binary==2.9.10
